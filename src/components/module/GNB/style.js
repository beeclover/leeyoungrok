import styled from 'styled-components';

export const Wrap = styled.div`
  box-shadow: 0px 3px 5px 1px rgba(0, 0, 0, 0.1);
  border-bottom: 1px solid ${({ theme }) => theme.palette.veryLightPink};
`;
